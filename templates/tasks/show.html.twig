{% extends 'tasks/showbase.html.twig' %}

{% block title %}Tasks{% endblock %}

{% block body %}
    <h1>Tasks</h1>

    <table class="table">
        <tbody>
        <tr>
            <th>Id</th>
            <td>{{ task.id }}</td>
        </tr>
        <tr>
            <th>Nom</th>
            <td>{{ task.nom }}</td>
        </tr>
        <tr>
            <th>Task_statue</th>
            <td>{{ task.taskStatue ? 'Yes' : 'No' }}</td>
        </tr>
        </tbody>
    </table>

    <a href="{{ path('tasks_index') }}">back to list</a>

    <a href="{{ path('tasks_edit', {'id': task.id}) }}">edit</a>

    {{ include('tasks/_delete_form.html.twig') }}
{% endblock %}

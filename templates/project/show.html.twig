{% extends 'project/showbase.html.twig' %}

{% block title %}Project{% endblock %}

{% block body %}
    <h1>Project</h1>

    <table class="table">
        <tbody>
        <tr>
            <th>Id</th>
            <td>{{ project.id }}</td>
        </tr>
        <tr>
            <th>Num_project</th>
            <td>{{ project.numProject }}</td>
        </tr>
        <tr>
            <th>Nom_project</th>
            <td>{{ project.NomProject }}</td>
        </tr>
        <tr>
            <th>Localisation</th>
            <td>{{ project.localisation }}</td>
        </tr>
        </tbody>
    </table>

    <div align="center">
        <a class="btn btn-primary" href="{{ path('project_index') }}">back to list</a>
        <a class="btn btn-primary" href="{{ path('project_edit', {'id': project.id}) }}">edit</a>
        <a class="btn btn-primary" href="{{ path('tasks_index') }}">Show tasks</a>
        <a class="btn btn-primary" href="{{ path('project_stats', {'id':project.id}) }}">Show Project stats</a>
        <a class="btn btn-primary" href="{{ path('messagerie') }}">Message Project Members</a>
    </div>
    <br>

    {{ include('project/_delete_form.html.twig') }}
{% endblock %}
